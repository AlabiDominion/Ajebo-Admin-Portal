namespace ShiftSolutions.web.Models
{
    public enum PropertyStatus { Pending = 0, Approved = 1, Rejected = 2 }
    public enum PropertyType { Shortlet = 0, Lease = 1 }
    public enum NotificationType { Approval = 0, Rejection = 1, Payment = 2, Invoice = 3, General = 4 }
}
